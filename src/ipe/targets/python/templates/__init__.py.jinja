"""{{ api_name }} API client.

Auto-generated by Ipê v{{ ipe_version }}.
"""

from {{ module_name }}.client import {{ api_name | class_name }}Client
{% for model in models | sort(attribute="name") %}
from {{ module_name }}.models.{{ model.name | module_name }} import {{ model.name | class_name }}
{% endfor %}

__all__ = [
{% for name in (models | sort(attribute="name") | map(attribute="name") | map("class_name") | list + [api_name | class_name ~ "Client"]) | sort %}
    "{{ name }}",
{% endfor %}
]
