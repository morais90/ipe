swagger: "2.0"
info:
  title: Swagger 2.0 API
  version: 1.0.0
host: api.example.com
basePath: /v1
schemes:
  - https
paths:
  /pets:
    get:
      summary: List pets
      produces:
        - application/json
      responses:
        200:
          description: OK
